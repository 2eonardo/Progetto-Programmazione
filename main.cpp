#include <iostream>
//(13) Classi che rappresentano note (es. Bear / Evernote).
// Ogni nota è composta da titolo e testo, può essere bloccata per evitarne
// cancellazione/modifica e fa parte di una collezione specificata con nome.
// Può anche far parte di una collezione di note importanti oltre che di una
// collezione specifica.
// Usare Observer per indicare quante note stanno in una collezione.
int main() {
    std::cout << "Prova 2" << std::endl;
    return 0;
}
